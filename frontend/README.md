## Getting Started

First, you must configure you propertly your `.env` file.

Then you can run `dev` environment:

```bash
## from project root
make dev-start
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.
